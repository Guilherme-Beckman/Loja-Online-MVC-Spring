<!DOCTYPE html>
<html lang="en">
	<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="style-register.css">
</head>
<body>
<div class="container-form" >
	<div class="form">
		<div th:if= "${param.error}">
			<div class="error-message">
				<p>Invalid Username or Password</p>
			</div>
		</div>
		
		<div th:if= "${param.logout}">
			<div class="logout-message">
				<p>Logout Sucess</p>
			</div>
		</div>
		
		<form 
		th:action="@{/login}" 
		th:object="${/user}" 
		method="post"
		rola="form">
			<h1>Login</h1>
			<label for="name">Digite seu email</label>
			<br>
			<input th:field="*{name}" type="email" name="name" id="name" placeholder="Ex: a12baj@gmail.com..." required th:field="*{name}">
			<br>
			<label for="password">Digite sua senha</label>
			<br>
			<input th:field="*{password}" type="password" name="password" id="password"  required th:field="*{password}">
			<br>
			<div class="submit-button">
    			<button type="submit">Enviar</button>
    		</div>
		</form>
		<span>Ainda não tem uma conta? Registre-se agora <a th:href="@{/register}">Registrar</a></span>
		
	</div>
	
</div>

</body>
</html>